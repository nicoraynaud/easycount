version: '2'
services:
    easycount-postgresql:
        image: postgres:9.6.2
        # volumes:
        #     - ~/volumes/jhipster/easycount/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=easycount
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
